# Source layout

The generated HTML pages live in `site/` and are rebuilt from scripts and data.
Page templates are currently embedded in the corresponding builders in `scripts/`.
This directory is reserved for shared templates and frontend assets as the site evolves.
